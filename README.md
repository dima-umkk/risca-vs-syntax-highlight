RiscA assembler syntax highligh extension for Visual Studio Code.

To install extension use `Extensions` section in VSCode, select `Install from VSIX...` and choose downloaded `.vsix
or
Use CLI:

```
code --install-extension your-extension-0.0.1.vsix
```